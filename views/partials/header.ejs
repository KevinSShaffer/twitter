<!DOCTYPE html>
<html>
<head>
	<title></title>
</head>
<body>
<% if (!user) { %>
	<a href="/login">Log In</a>
	<a href="/new">Register</a>
<% } else { %>
	<a href="/<%= user.username %>/edit">Signed in as: <strong><%= user.username %></strong></a>
	<a href="/logout">Logout</a>
<% } %>	